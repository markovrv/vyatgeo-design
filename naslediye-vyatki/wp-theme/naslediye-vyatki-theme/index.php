<?php
// До этого файла выполнение не доходит: весь фронтовый вывод перехватывается
// в functions.php (nv_spa_bootstrap на хуке template_redirect) раньше, чем WP
// загружает шаблон. Файл нужен только потому, что WordPress требует index.php
// в корне темы, чтобы считать её валидной.
